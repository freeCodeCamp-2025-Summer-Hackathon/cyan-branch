/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Allows using user's Google profile image
    domains: ['lh3.googleusercontent.com'],
  },
};

export default nextConfig;
